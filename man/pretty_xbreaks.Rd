% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pretty-axis.R
\name{pretty_xbreaks}
\alias{pretty_xbreaks}
\title{Generate pretty x-axis breaks}
\usage{
pretty_xbreaks(n = 5, breaks = NULL, include = NULL, discrete.expand = TRUE)
}
\arguments{
\item{n}{Numeric. Approximate number of breaks to generate when
\code{breaks} is \code{NULL}. Defaults to 5.}

\item{breaks}{Optional vector of x-axis breaks. If supplied, these
values are used directly.}

\item{include}{Optional numeric vector of values to include when
determining the range passed to \code{pretty()}.}

\item{discrete.expand}{Logical. Whether to retain the default
expansion when the x-axis is discrete. If \code{TRUE}, the x-axis
retains the default ggplot2 expansion. If \code{FALSE}, the x-axis
uses \code{expand = c(0, 0)}. Defaults to \code{TRUE}.}
}
\value{
An object of class \code{pretty_breaks}.
}
\description{
A convenience wrapper around \code{\link[=pretty_breaks]{pretty_breaks()}} for controlling
the x-axis.
}
\examples{
library(ggplot2)

ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  pretty_xbreaks()

ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  pretty_xbreaks(
    include = c(2, 4)
  )

ggplot(mtcars, aes(factor(cyl), mpg)) +
  geom_boxplot() +
  pretty_xbreaks(
    discrete.expand = FALSE
  )

}
\seealso{
\code{\link[=pretty_breaks]{pretty_breaks()}} for both axes.
\code{\link[=pretty_ybreaks]{pretty_ybreaks()}} for the y-axis.
}
