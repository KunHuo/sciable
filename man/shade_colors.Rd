% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-colors.R
\name{shade_colors}
\alias{shade_colors}
\title{Lighten or Darken Colors}
\usage{
shade_colors(colors, amount, space = c("HCL", "HLS", "combined"), plot = FALSE)
}
\arguments{
\item{colors}{A character vector of colors.}

\item{amount}{A numeric vector specifying the direction and magnitude of
color adjustment. Negative values lighten colors, positive values darken
colors, and zero values leave colors unchanged. Values must be between
-1 and 1.}

\item{space}{A character string specifying the color space used for
lightening. One of \code{"HCL"}, \code{"HLS"}, or \code{"combined"}. Positive values
are always processed using \code{"combined"} for darkening.}

\item{plot}{Logical. If \code{TRUE}, display the resulting colors using
\code{\link[=show_colors]{show_colors()}}. Defaults to \code{FALSE}.}
}
\value{
If \code{plot = FALSE}, a character vector containing the adjusted
colors. If \code{plot = TRUE}, a ggplot object returned by \code{\link[=show_colors]{show_colors()}}.
For each input color, the results corresponding to \code{amount} are returned
in the same order as supplied.
}
\description{
Lighten or darken colors according to the sign of the adjustment amount.
}
\details{
Negative values of \code{amount} are used to lighten colors, with their
absolute values passed to \code{\link[colorspace:lighten]{colorspace::lighten()}}. Positive values are
used to darken colors with \code{\link[colorspace:darken]{colorspace::darken()}} using the
\code{"combined"} method. Zero values return the original color unchanged.
}
\examples{
shade_colors(
  "#336699",
  c(-0.3, -0.15, 0, 0.15, 0.3)
)

shade_colors(
  "#336699",
  c(-0.3, -0.15, 0, 0.15, 0.3),
  plot = TRUE
)

}
\seealso{
\code{\link[=lighten_colors]{lighten_colors()}}, \code{\link[=darken_colors]{darken_colors()}}, \code{\link[=show_colors]{show_colors()}}
}
