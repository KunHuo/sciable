% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg-save.R
\name{reverse_x_axis}
\alias{reverse_x_axis}
\title{Reverse x-axis order (works with +)}
\usage{
reverse_x_axis(...)
}
\arguments{
\item{...}{Currently unused. Included for future compatibility.}
}
\value{
A ggplot object with reversed x-axis
}
\description{
Automatically handles both discrete (factor/character) and continuous (numeric) x variables.
No need to manually convert x to factor.
}
\examples{
\donttest{
# Numeric x variable
ggplot2::ggplot(mtcars, ggplot2::aes(cyl, mpg)) +
  ggplot2::geom_point() +
  reverse_x_axis()

# Character x variable
ggplot2::ggplot(mtcars, ggplot2::aes(as.character(cyl), mpg)) +
  ggplot2::geom_boxplot() +
  reverse_x_axis()

# Factor x variable
ggplot2::ggplot(mtcars, ggplot2::aes(factor(cyl), mpg)) +
  ggplot2::geom_boxplot() +
  reverse_x_axis()

# With bar plot
df <- data.frame(
  category = c("High", "Medium", "Low"),
  value = c(30, 50, 20)
)
ggplot2::ggplot(df, ggplot2::aes(category, value)) +
  ggplot2::geom_col() +
  reverse_x_axis()
}
}
