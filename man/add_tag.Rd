% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg-save.R
\name{add_tag}
\alias{add_tag}
\title{Add a tag to a ggplot}
\usage{
add_tag(label = "A", size = 9, face = "bold", color = "black")
}
\arguments{
\item{label}{A character string specifying the tag text. Default is "A".}

\item{size}{Numeric value for the font size of the tag. Default is 9.}

\item{face}{Character string for the font face (e.g., "bold", "italic").
Default is "bold".}

\item{color}{Character string for the text color. Default is "black".}
}
\value{
A list containing:
  \itemize{
    \item A `ggplot2::labs()` call to set the tag label.
    \item A `ggplot2::theme()` call to customize the tag appearance and
      position using `ggplot2::element_text()`.
  }
}
\description{
This function creates a list of ggplot2 components that adds a tag (e.g., "A",
"B", etc.) to the top-left corner of a plot. It combines a label with custom
text formatting and positioning. All ggplot2 functions are called with explicit
namespace prefix to avoid conflicts.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(x = wt, y = mpg)) + geom_point()
p + add_tag("A", size = 14, color = "blue")

}
