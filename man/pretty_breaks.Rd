% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pretty-axis.R
\name{pretty_breaks}
\alias{pretty_breaks}
\title{Generate pretty axis breaks for ggplot2}
\usage{
pretty_breaks(
  n = 5,
  xbreaks = NULL,
  ybreaks = NULL,
  xinclude = NULL,
  yinclude = NULL,
  discrete.expand = TRUE
)
}
\arguments{
\item{n}{Numeric. Approximate number of breaks to generate when
\code{xbreaks} or \code{ybreaks} is \code{NULL}. Defaults to 5.}

\item{xbreaks}{Optional vector of x-axis breaks. If supplied, these
values are used directly.}

\item{ybreaks}{Optional vector of y-axis breaks. If supplied, these
values are used directly.}

\item{xinclude}{Optional numeric vector of values to include when
determining the range passed to \code{pretty()} for the x-axis.}

\item{yinclude}{Optional numeric vector of values to include when
determining the range passed to \code{pretty()} for the y-axis.}

\item{discrete.expand}{Logical. Whether to retain the default
expansion for discrete axes. If \code{TRUE}, discrete axes retain the
default ggplot2 expansion. If \code{FALSE}, discrete axes use
\code{expand = c(0, 0)}. Defaults to \code{TRUE}.}
}
\value{
An object of class \code{pretty_breaks}.
}
\description{
Automatically generates and applies axis breaks for continuous or
discrete x and y axes. For continuous axes, values specified by
\code{xinclude} and \code{yinclude} are incorporated into the range before
\code{pretty()} calculates the final breaks.
}
\details{
If \code{xbreaks} or \code{ybreaks} is supplied, the corresponding axis uses
those breaks directly. The corresponding \code{xinclude} or \code{yinclude}
values are then ignored.

Continuous axes always use \code{expand = c(0, 0)}. For discrete axes,
\code{discrete.expand = TRUE} retains the default ggplot2 expansion,
whereas \code{discrete.expand = FALSE} uses \code{expand = c(0, 0)}.
}
\examples{
library(ggplot2)

ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  pretty_breaks()

ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  pretty_breaks(
    xbreaks = seq(1, 6, 1),
    ybreaks = seq(10, 35, 5)
  )

ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  pretty_breaks(
    xinclude = c(2, 4),
    yinclude = c(15, 30)
  )

ggplot(mtcars, aes(factor(cyl), mpg)) +
  geom_boxplot() +
  pretty_breaks()

ggplot(mtcars, aes(factor(cyl), mpg)) +
  geom_boxplot() +
  pretty_breaks(
    discrete.expand = FALSE
  )

}
\seealso{
\code{\link[=pretty_xbreaks]{pretty_xbreaks()}} for the x-axis only.
\code{\link[=pretty_ybreaks]{pretty_ybreaks()}} for the y-axis only.
}
